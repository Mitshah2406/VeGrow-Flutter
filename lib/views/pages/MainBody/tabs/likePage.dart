import 'package:flutter/material.dart';

class LikePage extends StatelessWidget {
  LikePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Hello Like"),
    );
  }
}